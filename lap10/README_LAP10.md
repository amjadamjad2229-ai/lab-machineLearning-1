Lap 10 (SVM) update notes

- `01-Support Vector Machines.ipynb`: breast cancer SVM + GridSearch example exists.
- `02-SVM Assignment.ipynb`: currently only contains assignment prompt text; code cells still need to be inserted.

Files created for validation:
- `02-SVM_Assignment_solution_cells.py`: a working script that completes the missing iris SVM steps (plots + train/eval + GridSearch).

